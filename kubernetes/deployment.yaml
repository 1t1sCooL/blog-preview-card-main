apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-preview-card-main-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blog-preview-card-main-app
  template:
    metadata:
      labels:
        app: blog-preview-card-main-app
    spec:
      containers:
        - name: blog-preview-card-main-app
          image: blog-preview-card-main:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
